# Policy (#279): 4h idle expiry, 8h hard cap. Bounds passphrase reuse to a
# work session; 12h spanned overnight unattended time. Single-user FDE
# workstation with screen lock, so shorter buys little.
default-cache-ttl 14400
max-cache-ttl 28800
pinentry-program /opt/homebrew/bin/pinentry-tty
